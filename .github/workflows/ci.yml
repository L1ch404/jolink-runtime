name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: ${{ matrix.os }} / Python ${{ matrix.python }} / JDK ${{ matrix.java }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            python: "3.11"
            java: "8"
          - os: ubuntu-latest
            python: "3.11"
            java: "17"
          - os: ubuntu-latest
            python: "3.13"
            java: "17"
          - os: ubuntu-latest
            python: "3.11"
            java: "21"
          - os: windows-latest
            python: "3.11"
            java: "8"
          - os: windows-latest
            python: "3.11"
            java: "17"
          - os: macos-latest
            python: "3.11"
            java: "17"

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - run: uv sync --extra dev --locked
      - name: Verify frozen Runtime lineage
        if: matrix.os == 'ubuntu-latest' && matrix.python == '3.11' && matrix.java == '17'
        run: uv run pytest -q tests/contract/test_runtime_lineage_golden.py
      - name: Run portable test suite
        run: uv run pytest -m "not mcp_java_e2e and not fast_test_e2e and not gradle_runtime_e2e"
      - name: Run real MCP/JVM safety E2E
        if: matrix.os == 'ubuntu-latest' && matrix.python == '3.11' && matrix.java == '17'
        env:
          JOLINK_RUN_MCP_JAVA_E2E: "1"
        run: uv run pytest -m mcp_java_e2e tests/e2e/test_stdio_mcp_java.py

  distribution:
    name: ${{ matrix.os }} installed product MCP E2E / target JDK ${{ matrix.java }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            java: "17"
          - os: windows-latest
            java: "8"
          - os: windows-latest
            java: "17"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - name: Build wheel and sdist without workspace sources
        run: uv build --no-sources --clear
      - name: Verify wheel and sdist from clean environments
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $version = uv run python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])"
          $wheel = (Get-ChildItem "dist/*.whl").FullName
          $sdist = (Get-ChildItem "dist/*.tar.gz").FullName
          foreach ($artifact in @($wheel, $sdist)) {
            $kind = if ($artifact.EndsWith(".whl")) { "wheel" } else { "sdist" }
            $environment = Join-Path $env:RUNNER_TEMP "jolink-$kind"
            uv venv $environment --python 3.11
            $python = Join-Path $environment $(if ($IsWindows) { "Scripts/python.exe" } else { "bin/python" })
            $server = Join-Path $environment $(if ($IsWindows) { "Scripts/jolink-runtime.exe" } else { "bin/jolink-runtime" })
            uv pip install --python $python $artifact
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
            & $python "scripts/verify_distribution.py" `
              --server $server `
              --expected-source-root $env:GITHUB_WORKSPACE `
              --expected-version $version
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }
